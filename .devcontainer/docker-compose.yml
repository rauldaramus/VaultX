version: '3.8'

services:
  # Development container
  app:
    container_name: nextjs_dev
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../..:/workspaces:cached
    command: sleep infinity
